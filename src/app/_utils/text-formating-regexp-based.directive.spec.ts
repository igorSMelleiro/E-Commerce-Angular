import { TextFormatingRegexpBasedDirective } from './text-formating-regexp-based.directive';

describe('TextFormatingRegexpBasedDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFormatingRegexpBasedDirective();
    expect(directive).toBeTruthy();
  });
});
